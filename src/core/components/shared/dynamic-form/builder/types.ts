import { IBuilderField } from '../interfaces/builder-field.interface';
import { IFieldGroup } from '../interfaces/field-group.interface';

/**
 * Re-export builder types for convenience within the builder module.
 */
export type BuilderField = IBuilderField;
export type FieldGroup = IFieldGroup;
