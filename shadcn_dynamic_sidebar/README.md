# Dynamic ShadCN Sidebar

This is a React implementation of a dynamic, configurable sidebar using ShadCN UI components. It supports multiple variants (simple, collapsible, floating, etc) via props and loads its menu structure from a config file. The design follows ShadCN UI accessibility and responsive standards.
...
